# test-project-new — Context

## Elevator pitch
<1-2 sentences. Auto-populate from architecture.md after /create-plan, or user fills now.>

## Current phase
Phase 3 — Auth state and navigation skeleton (complete). All stories 3.1–3.7 done on `feat/phase-3-auth-navigation`. Story 3.1 was merged early via PR #22 (workflow violation — see Active blockers). Stories 3.2–3.7 are pending review on the phase PR opened by the dispatcher. Awaiting user merge of that phase PR, `phase-2-complete` and `phase-3-complete` tag pushes, and phase 4 `ready: true` flip.

## Active blockers
- None. (`$ANDROID_HOME` was set at Windows User scope to `C:\Users\syede\AppData\Local\Android\Sdk` on 2026-05-01 and is now visible to the bash subshell — verified before the phase 1 re-dispatch. `$ANDROID_SDK_ROOT` remains unset; not required by architecture.md or the phase 1 PRD.)

## Critical design decisions
- 2026-05-01: Node pin raised from `20.x LTS` to `24.x` to match the host (`v24.14.1`). Expo SDK 55 was originally evaluated against Node 20; Node 24 may not be officially supported. If `npx expo` install or bundler breaks at any point, suspect Node first and revert to Node 20 LTS via `nvm`. Recorded in architecture.md (pinned-version table + Open questions).

## Recent changes
- 2026-05-05: story 2.0 complete — @react-native-async-storage/async-storage@2.2.0 and babel-plugin-module-resolver installed via expo install; src/ created at project root; Helper/, labels/, styles/, navigation/ placeholder folders deleted; tsconfig.json updated with baseUrl:"." and paths @/*->src/*; babel.config.js created with expo/internal/babel-preset + module-resolver alias @->./src; jest.config.js updated with moduleNameMapper ^@/(.*)$-><rootDir>/src/$1 and phase-1 exclude comment amended to point to phase 3; @types/jest installed (pre-existing tsc TS2582 error on smoke.test.ts resolved); alias probe verified end-to-end then deleted; all four gates pass: lint, format:check, npm test, tsc --noEmit.
- 2026-05-05: story 2.1 complete — faithful transcription of theme.md §14 into src/theme/theme.ts, src/theme/typography.ts, src/theme/ThemeProvider.tsx, src/theme/index.ts; 15 unit tests covering all AC token/typography assertions plus ThemeProvider inside/outside-provider paths; all four gates pass (lint 0 errors, format clean, 15/15 tests, tsc --noEmit exit 0); coverage 100% stmts/funcs/lines, 83% branches (>75% threshold); App.tsx unmodified, no font installs, no new library installs.
- 2026-05-07: story 2.2 complete — created src/labels/labels.json with all 32 v1 label keys (app_title, ok_button, cancel_button, 9 login_*, 18 register_*, 2 registration_success_*, landing_screen_title, landing_tab_home_label); added __tests__/labels.test.ts asserting key presence and non-empty en values; all four gates pass (lint 0, format clean, 17/17 tests, tsc --noEmit exit 0); coverage thresholds hold; no screens import labels.json.
- 2026-05-07: story 2.3 complete — created src/Helper/seedCredentials.ts (capital H) exporting SEED_CREDENTIAL: SeedCredential typed constant with username "testuser" and password "Test@123"; no side effects on import; added __tests__/seedCredentials.test.ts with 3 tests (username exact, password exact, object shape); 20/20 tests pass, coverage 100%/83%/100%/100% (all above thresholds), lint 0, tsc --noEmit exit 0; pre-existing format:check failures on 11 files are unchanged from origin/development baseline.
- 2026-05-07: chore (pre-2.4 interstitial) — added `"endOfLine": "auto"` to .prettierrc to fix Windows CRLF regression; root cause is phase-1 bootstrap omitting endOfLine; format:check had been failing on 13 files (all stories 2.0–2.3 output) after git checkout on Windows with core.autocrlf=true converts LF to CRLF; no source files were rewritten by prettier --write (all unchanged); all four gates pass post-fix: lint 0, format:check exit 0, 20/20 tests, tsc --noEmit exit 0; this is a phase-1 oversight surfaced by stories 2.0–2.3, not a story regression.
- 2026-05-07: story 2.4 complete — created src/Helper/storageHelper.ts (capital H) exporting StoredUser interface and readUsers()/writeUsers() keyed under @test-project-new/registered-users; error wrapping uses ES2022 Error cause; __tests__/storageHelper.test.ts covers all 4 ACs (fresh storage →[], round-trip write/read, getItem throw → storage_read_failed with cause, setItem throw → storage_write_failed with cause); 27/27 tests pass, coverage 100%/85.71%/100%/100% (all above thresholds), lint 0, format:check clean, tsc --noEmit exit 0.
- 2026-05-05: story 2.5 complete — created src/Helper/validationHelper.ts exporting ValidationResult type and validateUsername/validateEmail/validatePassword/validateConfirmPassword; 51 new tests covering all boundary cases (empty, whitespace, below-min, exactly-min/max, above-max, invalid chars, missing letter/digit, confirm no-trim mismatch); 79/79 tests pass, validationHelper.ts 100%/100%/100%/100%, global 100%/94.73%/100%/100% (all above thresholds); lint 0 warnings, format:check exit 0, tsc --noEmit exit 0; PR #16 open.
- 2026-05-07: story 2.6 complete — created src/Helper/credentialHelper.ts exporting lookupCredential(); lookup order: registered users first (via readUsers), then SEED_CREDENTIAL; username trimmed+lowercased, password exact; 9 unit tests in __tests__/credentialHelper.test.ts covering all ACs including the precedence invariant (registered match short-circuits seed) and case/whitespace normalisation; 88/88 tests pass, credentialHelper.ts 100%/87.5%/100%/100% (all above thresholds), lint 0, format:check exit 0, tsc --noEmit exit 0.
- 2026-05-07: story 2.7 Leg A complete — harness in place, App.tsx temp-edited, awaiting user AVD checkpoint.
- 2026-05-07: story 2.7 Leg B complete — App.tsx reverted to phase-1 baseline (b6ce83d), src/StorageSmokeTest.tsx and its unit test deleted. Phase 2 stories all done.
- 2026-05-07: phase 2 complete — all 8 stories shipped (2.0 prep+aliases, 2.1 theme/*, 2.2 labels.json, 2.3 seedCredentials, 2.4 storageHelper, 2.5 validationHelper, 2.6 credentialHelper, 2.7 AVD smoke verified) plus chore PR #14 (prettier endOfLine) and bookkeeping PR #17. Thin index flipped phase 2 done: true.
- 2026-05-08: story 3.1 complete — created src/screens/LoginScreen.tsx, src/screens/RegisterScreen.tsx, src/screens/LandingScreen.tsx (stub components with testIDs login-screen-stub, register-screen-stub, landing-screen-stub; label text from labels.json via direct import); added __tests__/screens.stubs.test.tsx with 6 tests (testID + label text per stub); all gates pass: lint 0 errors, format:check exit 0, 94/94 tests pass (8 new), tsc --noEmit exit 0; coverage: 100% stmts/funcs/lines, 93.47% branches (threshold 75%); branch feat/phase-3-auth-navigation created.
- 2026-05-08: story 3.2 complete — created src/auth/AuthContext.tsx (AuthContextValue interface, AuthProvider component, useAuth hook; in-memory useState<boolean>(false), signIn/signOut via useCallback, value via useMemo; context object module-private; useAuth throws exact string 'useAuth must be used within an AuthProvider' when called outside provider); added __tests__/AuthContext.test.tsx with 4 tests (initial unauthenticated, signIn transitions authenticated, signOut transitions back, throw-outside-provider exact message verified); all gates pass: lint 0 errors/warnings, format:check exit 0, 98/98 tests pass, tsc --noEmit exit 0; coverage: 100% stmts/funcs/lines, 93.75% branches (AuthContext.tsx 100%/100%/100%/100%).
- 2026-05-08: story 3.3 complete — installed 5 navigation packages via `npx expo install`: @react-navigation/native@7.2.3, @react-navigation/native-stack@7.14.13, @react-navigation/bottom-tabs@7.15.12, react-native-screens@4.23.0 (>=v3, enableScreens() not required), react-native-safe-area-context@5.6.2; architecture.md pinned-version table updated with resolved @react-navigation/native@7.2.3 row (all 5 package versions recorded), Open Questions §254 ("React Navigation major version") removed, version-pin policy paragraph updated; created src/navigation/AuthRoutes.tsx (AUTH_ROUTES const, AuthStackParamList type, createNativeStackNavigator<AuthStackParamList> with Login+Register screens, initialRouteName=AUTH_ROUTES.LOGIN); added __tests__/AuthRoutes.test.tsx with 1 test (NavigationContainer render asserts login-screen-stub in tree); jest-expo transformIgnorePatterns confirmed to include @react-navigation (N2 non-issue); all gates pass: lint 0 errors/warnings, format:check exit 0, 99/99 tests pass (10 suites), tsc --noEmit exit 0; coverage: 100% stmts/funcs/lines, 93.75% branches (AuthRoutes.tsx 100%/100%/100%/100%, all above thresholds).
- 2026-05-08: story 3.4 complete — created src/navigation/AppRoutes.tsx (APP_ROUTES const {LANDING:'Landing'}, AppTabParamList type, createBottomTabNavigator<AppTabParamList> with single Landing tab, tabBarLabel wired to labels.landing_tab_home_label.en, tab bar intentionally visible in v1); added __tests__/AppRoutes.test.tsx with 2 tests (landing-screen-stub testID in tree; landing_tab_home_label.en text in tree via getAllByText — F5 tab-label assertion confirmed; note: both landing_tab_home_label.en and landing_screen_title.en resolve to "Home" causing multiple matches, handled with getAllByText length > 0); all gates pass: lint 0 errors/warnings, format:check exit 0, 101/101 tests pass (11 suites), tsc --noEmit exit 0; coverage: 100% stmts/funcs/lines, 93.75% branches (AppRoutes.tsx 100%/100%/100%/100%, all above thresholds); no PR opened (story 3.4 is not the final story of the phase).
- 2026-05-08: story 3.5 complete — created src/navigation/AppNavigator.tsx (useAuth() ternary top-level branch: unauthenticated→AuthRoutes, authenticated→AppRoutes; single NavigationContainer wrapper; inactive stack fully unmounted); App.tsx overwritten with AuthProvider+AppNavigator+StatusBar and old View/StyleSheet content removed; added __tests__/AppNavigator.test.tsx with 2 tests using SignInTrigger harness pattern (unauthenticated state: login-screen-stub present, landing-screen-stub null; authenticated state after signIn: landing-screen-stub present, login-screen-stub null); auth-state test pattern: SignInTrigger sibling component inside AuthProvider calls signIn() via fireEvent.press inside act(); all gates pass: lint 0 errors/warnings, format:check exit 0, 103/103 tests pass (12 suites), tsc --noEmit exit 0; coverage: 100% stmts/funcs/lines, 94% branches (AppNavigator.tsx 100%/100%/100%/100%); no PR opened (story 3.5 is not the final story of the phase).
- 2026-05-08: story 3.7 complete — created __tests__/AuthGate.test.tsx with 3 formal auth-gate architecture-guarantee tests: (1) unauthenticated default state: queryByTestId('landing-screen-stub') returns null, login-screen-stub present; (2) after signIn(): landing-screen-stub present, queryByTestId('login-screen-stub') returns null; (3) toggle within same render: pre-auth asserted, signIn fired, post-auth re-asserted (unmount/remount guarantee verified); SignInTrigger+AuthGateRoot harness pattern used; render-tree assertions only, no navigation calls per architecture.md "Auth-gate test technique"; 3 new tests (story-local), 107 total across 14 suites; all gates pass: lint 0 errors/warnings, format:check exit 0, 107/107 tests pass, tsc --noEmit exit 0; coverage: 100% stmts/funcs/lines, 94% branches; PRD story 3.7 done: true, last_updated bumped to 2026-05-08 (story 3.7); no PR opened — dispatcher handles phase PR.
- 2026-05-08: story 3.6 complete — AC1 (code-review only): LoginScreen.tsx imports are `import React from 'react'`, `import { View, Text } from 'react-native'`, `import labels from '@/labels/labels.json'` — no BackHandler import, no useFocusEffect import; confirmed by file inspection. AC2 (code-review only): LandingScreen.tsx imports are identical — no BackHandler import, no useFocusEffect import; confirmed by file inspection. AC3 (test-backed): created __tests__/AuthRoutes.backbutton.test.tsx using Technique A (createNavigationContainerRef) + spy-capture pattern — jest-expo uses iOS platform where BackHandler.addEventListener is a no-op, but React Navigation's useBackButton.native.js still calls it on mount; spy intercepts the handler, Technique A navigates to Register via navRef.navigate(AUTH_ROUTES.REGISTER) inside act(), then the captured handler is invoked inside act() to simulate hardware back, asserting register-screen-stub is gone and login-screen-stub is restored; 1 new test (story-local), 104 total across 13 suites; all gates pass: lint 0 errors/warnings, format:check exit 0, 104/104 tests pass, tsc --noEmit exit 0; coverage: 100% stmts/funcs/lines, 94% branches (all above thresholds); no PR opened (story 3.6 is not the final story of the phase).
- 2026-05-08: phase 3 complete — all 7 stories shipped (3.1 stub screens, 3.2 auth context with exact-string throw guard, 3.3 react-navigation install + AuthRoutes pre-auth stack + architecture.md §254 closed, 3.4 AppRoutes bottom-tab + F5 tab-label assertion, 3.5 AppNavigator auth-gated unmount swap + App.tsx wired with AuthProvider+StatusBar, 3.6 LoginScreen/LandingScreen no-handler verified by inspection + Register-pops-Login test via spy-capture, 3.7 formal auth-gate render-tree tests proving stack-swap-by-unmount). 107 tests across 14 suites, 100% stmts/funcs/lines, 94% branches. Workflow violation noted: PR #22 was opened by the 3.1 subagent against the gitbranching.md rule; user merged it as a one-off. Stories 3.2–3.7 followed the per-phase-PR rule strictly. Phase PR for 3.2–3.7 opened by dispatcher (this turn). Thin index flipped phase 3 done: true, last_updated 2026-05-08.
