# test-project-new — Context

## Elevator pitch
<1-2 sentences. Auto-populate from architecture.md after /create-plan, or user fills now.>

## Current phase
Phase 5 — Login screen (complete). All stories 5.1–5.3 done on `feat/phase-5-login-screen`. PR #28 is open against `development` with title `feat: phase 5 — login screen`. Awaiting user merge of PR #28, `phase-5-complete` tag push, and phase 6 `ready: true` flip.

## Active blockers
- None. (`$ANDROID_HOME` was set at Windows User scope to `C:\Users\syede\AppData\Local\Android\Sdk` on 2026-05-01 and is now visible to the bash subshell — verified before the phase 1 re-dispatch. `$ANDROID_SDK_ROOT` remains unset; not required by architecture.md or the phase 1 PRD.)

## Critical design decisions
- 2026-05-01: Node pin raised from `20.x LTS` to `24.x` to match the host (`v24.14.1`). Expo SDK 55 was originally evaluated against Node 20; Node 24 may not be officially supported. If `npx expo` install or bundler breaks at any point, suspect Node first and revert to Node 20 LTS via `nvm`. Recorded in architecture.md (pinned-version table + Open questions).

## Recent changes
- 2026-05-08: phase 3 complete — all 7 stories shipped (3.1 stub screens, 3.2 auth context with exact-string throw guard, 3.3 react-navigation install + AuthRoutes pre-auth stack + architecture.md §254 closed, 3.4 AppRoutes bottom-tab + F5 tab-label assertion, 3.5 AppNavigator auth-gated unmount swap + App.tsx wired with AuthProvider+StatusBar, 3.6 LoginScreen/LandingScreen no-handler verified by inspection + Register-pops-Login test via spy-capture, 3.7 formal auth-gate render-tree tests proving stack-swap-by-unmount). 107 tests across 14 suites, 100% stmts/funcs/lines, 94% branches. Workflow violation noted: PR #22 was opened by the 3.1 subagent against the gitbranching.md rule; user merged it as a one-off. Stories 3.2–3.7 followed the per-phase-PR rule strictly. Phase PR for 3.2–3.7 opened by dispatcher (this turn). Thin index flipped phase 3 done: true, last_updated 2026-05-08.
- 2026-05-08: story 4.1 complete — replaced RegisterScreen stub (testID register-screen-stub) with the real form: KeyboardAvoidingView root (testID register-screen), ScrollView, four fields (email/username/password/confirmPassword) each with label+TextInput+conditional inline error; validation via validationHelper fires on blur for all fields; confirmPassword mismatch fires on blur only (not on keystroke) per architecture.md; all copy from labels.json, all tokens from useTheme()+textStyles via createStyles(theme) factory; AuthRoutes.backbutton.test.tsx and screens.stubs.test.tsx updated to new testID and wrapped with ThemeProvider; 21 new component tests in __tests__/RegisterScreen.test.tsx; 128 total tests across 15 suites pass; coverage 97.97% stmts/91.46% branches/100% funcs/97.93% lines (all above thresholds); lint 0, format:check exit 0, tsc --noEmit exit 0; committed on feat/phase-4-register-screen.
- 2026-05-09: story 4.2 complete — wired submit handler on RegisterScreen: full validationHelper suite on press, readUsers() uniqueness check (registered list lowercased + SEED_CREDENTIAL.username reserved), setUsernameError/setEmailError for collisions, writeUsers([...existing, newRecord]) with trimmed original-case username, lowercased email, plaintext password, ISO 8601 createdAt; storage write failure renders register_storage_error inline (form stays editable); success state deferred to story 4.3 per AC authority; submit button (TouchableOpacity, testID register-submit-button, register_button label); screen title moved to testID register-screen-title to resolve getByText ambiguity with button copy; jest.setup.ts global AsyncStorage mock added (setupFilesAfterEnv) to fix cascading NativeModule: null failures in AuthRoutes, AuthGate, AppNavigator, screens.stubs suites when RegisterScreen transitively imported storageHelper; AuthRoutes.tsx initial route corrected to LOGIN (story 4.1 regression); AuthRoutes.test.tsx, AuthGate.test.tsx, AppNavigator.test.tsx wrapped with ThemeProvider; spy-based test asserts writeUsers called exactly once with StoredUser shape; mock rejection test asserts register_storage_error renders and form remains interactive; 12 new story-4.2 tests; 140 tests total, 15 suites; coverage 97.34% stmts/93.26% branches/100% funcs/97.26% lines (all above thresholds); lint 0, format:check exit 0, tsc --noEmit exit 0.
- 2026-05-08: story 4.3 complete — introduced `success` useState<boolean>(false) in RegisterScreen, set to true after writeUsers resolves; added RN built-in `<Modal onRequestClose={handleSuccess} visible={success} transparent animationType="fade">` as the sole success UX path; `handleSuccess` synchronously clears success and calls navigation.navigate(AUTH_ROUTES.LOGIN) via useNavigation<NativeStackNavigationProp<AuthStackParamList>>(); added `colors.overlay` token to lightColors and darkColors in theme.ts to avoid raw rgba literal in component; no Pressable backdrop, no onBackdropPress, no auto-timeout — only OK button and onRequestClose can dismiss; added 4 story-4.3 tests: modal absent before submit, modal renders after submit, OK navigates to Login, onRequestClose navigates to Login; fixed screens.stubs.test.tsx to mock useNavigation after RegisterScreen gained navigation dependency; 144 tests total, 15 suites, all pass; coverage 97.42% stmts/93.26% branches/100% funcs/97.35% lines (all above thresholds); lint 0, prettier clean, tsc --noEmit exit 0; committed on feat/phase-4-register-screen; phase PR opened against development.
- 2026-05-09: story 5.1 complete — replaced LoginScreen stub (testID login-screen-stub) with real layout: KeyboardAvoidingView root (testID login-screen), ScrollView, username field (autoCapitalize=none, testID login-username-input) and password field (secureTextEntry=true, testID login-password-input), login button (testID login-submit-button), Register link (testID login-register-link) calling navigation.navigate(AUTH_ROUTES.REGISTER); all copy from labels.json, all tokens from useTheme()+textStyles via createStyles(theme) factory mirroring RegisterScreen; updated login-screen-stub → login-screen in 5 existing test suites: __tests__/AppNavigator.test.tsx, __tests__/AuthGate.test.tsx, __tests__/AuthRoutes.test.tsx, __tests__/AuthRoutes.backbutton.test.tsx, __tests__/screens.stubs.test.tsx (no additional suites found via grep); created __tests__/LoginScreen.test.tsx with 9 new component tests; 153 tests total across 16 suites; coverage 97.51% stmts/92.45% branches/100% funcs/97.43% lines (all above thresholds); lint 0, format:check clean, tsc --noEmit exit 0; branch feat/phase-5-login-screen; submit handling/credential lookup/error UI deferred to stories 5.2 and 5.3.
- 2026-05-09: story 5.2 complete — wired login submit handler in LoginScreen.tsx: lookupCredential(trimmedUsername, passwordAsTyped), signIn() on ok:true, setStorageErrorVisible(true) on rejection (catch); storageErrorVisible declared as separate useState<boolean>(false) (UI deferred to story 5.3); no navigation.navigate on post-auth route; 7 new tests in __tests__/LoginScreen.test.tsx (success registered user, unknown username failure, wrong password failure, case-insensitive match, whitespace-trimmed match, registered-precedence-over-seed, storage rejection); 2 architecture-anchor full-harness tests in __tests__/AppNavigator.test.tsx (seed login → landing-screen-stub mounts, registered-precedence → pre-auth remains); AuthProvider wrapper added to AuthRoutes.test.tsx, AuthRoutes.backbutton.test.tsx, screens.stubs.test.tsx (LoginScreen now calls useAuth()); 162 tests total (+9), 16 suites; coverage 97.65% stmts/92.59% branches/100% funcs/97.58% lines; lint 0/0, format:check clean, tsc --noEmit exit 0; branch feat/phase-5-login-screen; PR #28 updated.
- 2026-05-09: story 5.3 complete — added credentialErrorVisible state (bool) and storageErrorVisible UI binding to LoginScreen.tsx; two independent error Text elements: login-inline-error (ok:false result, below button, login_invalid_credentials.en) and login-storage-error (AsyncStorage rejection, login_storage_error.en); handleUsernameChange/handlePasswordChange wrappers auto-clear credentialError on field change; createStyles factory gains inlineError+storageErrorText using theme.colors.status.error; 11 new story-5.3 tests in __tests__/LoginScreen.test.tsx; 173 tests total (+11), 16 suites; coverage 97.78% stmts/93.1% branches/100% funcs/97.72% lines; lint 0/0, format:check clean, tsc --noEmit exit 0; committed on feat/phase-5-login-screen.
- 2026-05-09: phase 5 complete — all 3 stories shipped on branch feat/phase-5-login-screen. 5.1: real LoginScreen layout (2 fields, button, register link, all copy+tokens from labels/theme). 5.2: submit handler wired — lookupCredential, signIn on success, storageErrorVisible catch. 5.3: two error surfaces (login-inline-error below button; login-storage-error independent), auto-clear on field change, genericness confirmed. Files: src/screens/LoginScreen.tsx, __tests__/LoginScreen.test.tsx (created 5.1, extended 5.2+5.3). Total tests added across phase: +27 (9+9+11 including AppNavigator anchors). Final coverage: 97.78% stmts/93.1% branches. PR #28 open against development, title feat: phase 5 — login screen.
