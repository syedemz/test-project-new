import React from 'react';

import StorageSmokeTest from '@/StorageSmokeTest';

// TEMPORARY — story 2.7 Leg A smoke harness.
// Reverted to the phase-1 template in story 2.7 Leg B after AVD verification.
export default function App(): React.JSX.Element {
  return <StorageSmokeTest />;
}
