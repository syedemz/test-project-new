phase: 6
title: Landing screen and end-to-end glue
last_updated: 2026-05-09

context_summary: |
  Replace the LandingScreen stub with a v1 profile-card landing screen that
  renders a full sample profile (Jasmin, 28) inside the bottom tab navigator
  already wired in phase 3. The layout is a ScrollView with a fixed transparent
  header overlaying the top of the scroll content and a fixed gold-banded
  banner overlaying the bottom — both absolutely positioned siblings of the
  ScrollView. All content is driven by a typed dummy dataset
  (src/data/sampleProfile.ts); section/button copy lives in labels/labels.json;
  all colors/spacing/typography come from the existing theme. After this phase
  we run the manual end-to-end test on the Android emulator and v1 MVP is
  feature-complete. Note: this scope intentionally diverges from
  architecture.md's earlier "empty landing for v1" contract — captured in
  Workspace lessons applied / project decisions for phase 6.

stories:
  - id: 6.1
    title: Build LandingScreen v1 — full profile-card layout (Jasmin sample) with fixed transparent header and fixed gold bottom banner
    agent: frontenddeveloper
    done: false
    depends_on: []
    acceptance_criteria:
      - "[layout root] `screens/LandingScreen.tsx` renders three siblings inside a root `<View style={{ flex: 1 }}>`: (a) a single full-screen `<ScrollView testID=\"landing-screen\">` whose contentContainerStyle has `paddingBottom` equal to the bottom banner's measured height (capture via onLayout) so the last scroll item can rise fully above the banner; (b) `<View testID=\"landing-header\">` with `position: 'absolute', top: 0, left: 0, right: 0` and a transparent backgroundColor — sits above the ScrollView in z-order; (c) `<View testID=\"landing-bottom-banner\">` with `position: 'absolute', bottom: 0, left: 0, right: 0`. The hero photo is the first item in the ScrollView and extends to the top of the screen — it sits behind the transparent header. The header does NOT change opacity on scroll for v1."

      - "[header content] The header renders an X close icon on the left (`testID=\"landing-header-close\"`, `theme.colors.text.inverse` color while overlapping the photo region) and a center title group (`testID=\"landing-header-title\"`) containing `SAMPLE_PROFILE.name` + a verified-tick icon (Unicode ✓ inside a circle, or an emoji-equivalent — implementer choice as long as it reads as 'verified'). For v1 the X tap calls `useAuth().signOut()` so the user can exit the post-auth state. Header content respects the OS top safe-area inset (use `react-native-safe-area-context` hooks if not already imported)."

      - "[bottom banner content] The bottom banner is a single absolutely-positioned container with two stacked rows. Top row: a SOLID gold band using `theme.colors.text.premium` as backgroundColor (gold in light/dark) with a small crown emoji + uppercase `landing_banner_overline` label (e.g. `REVEAL JASMIN'S FILTERS`) in `theme.colors.text.inverse`. Bottom row: a card with backgroundColor `theme.colors.bg.premium`, padding `xl` horizontal / `md` vertical, containing on the left a two-line text from `landing_banner_text` (`Change your mind and give Jasmin a second chance!`) and on the right a pill button (`testID=\"landing-banner-like-button\"`, `borderRadius: theme.radii.pill`, backgroundColor `bg.surface`, label from `landing_banner_action_label`). Like-button onPress is a no-op for v1 (logs to console.log only)."

      - "[hero block] The first ScrollView item is `<View testID=\"landing-hero\">` rendering `<Image source={SAMPLE_PROFILE.photoUri} />` at full screen width; height = `Dimensions.get('window').width * 1.25`. The photo's lower 40% is darkened by an absolutely-positioned overlay `<View>` at `rgba(0,0,0,0.45)` (no gradient library) — over which the following text sits in `theme.colors.text.inverse`: line 1 — name `Jasmin` in `textStyles.display.lg` + age `28` in `textStyles.display.md` + a verified-tick glyph; line 2 — `🇩🇪 276 KM AWAY, BOGENHAUSEN` in caption style (uppercased); line 3 — a horizontal-wrapping flex row of `topChips`. All values come from `SAMPLE_PROFILE.location` and `SAMPLE_PROFILE.topChips`."

      - "[scroll body sections] Below the hero, the ScrollView renders these sections in this exact order, each as `<View testID=\"landing-section-<slug>\">` with title text from labels.json in `textStyles.heading.lg`/`text.primary`, then the section body: (1) `your-similarities` — description text + chip row from `SAMPLE_PROFILE.similarities`; (2) `about-me` — chip row; (3) `marriage-intentions` — a card (`testID=\"landing-card-marriage-intentions\"`, backgroundColor `bg.muted`, `borderRadius: radii.lg`, padding `xl`) containing the title, then a horizontal track widget — heart icon + 'Match!' label on the left (color `accent.primary`), a 1px line in `border.default` spanning the row, and a dark pill on the right with the marriage icon + label (backgroundColor `bg.surface` for dark mode parity OR `text.primary` with inverse text — implementer choice as long as it reads as a 'dark chip' against the card), with `1-2 years` caption beneath the right end; (4) `faith` — chip row; (5) `future-plans` — chip row; (6) `interests` — chip row (wraps across multiple lines); (7) `personality` — chip row; (8) `education` — chip row; (9) `languages` — chip row; (10) `bio` — title + body text (placeholder `...` for v1, sourced from `SAMPLE_PROFILE.bio`); (11) `verified-profile` — a card (`testID=\"landing-card-verified-profile\"`, same card style as marriage-intentions) with title + an info icon (ⓘ) + body text from `landing_section_verified_profile_body_template` interpolating `SAMPLE_PROFILE.name`; (12) `compliment` — uppercase overline `landing_compliment_overline` + heading `landing_compliment_title` + multi-line `<TextInput testID=\"landing-compliment-input\">` (height ~96, multiline=true, `borderRadius: radii.md`, `borderWidth: 1`, `borderColor: border.default`, padding `lg`, placeholder from `landing_compliment_placeholder`) + a full-width pill Submit button (`testID=\"landing-compliment-submit\"`, `borderRadius: radii.pill`, backgroundColor `accent.primary`, color `text.inverse`); Submit onPress is a no-op for v1; (13) `actions-footer` — a centered row 'Share profile' link with a share-icon glyph (`testID=\"landing-action-share\"`), then a 3-column row of icon-over-label buttons Favourite / Block / Report (`testID=\"landing-action-favourite\"`, `landing-action-block`, `landing-action-report`). All footer onPress handlers are no-ops in v1. Section vertical spacing: `theme.spacing.xxxl` between sections, `xl` between a title and its body."

      - "[chip primitive] In `src/screens/LandingScreen.tsx` (or a sibling file under `src/screens/landing/`), implement a local `Chip` subcomponent: `<Chip icon={emoji} label={text} />`. Visual: backgroundColor `bg.muted`, `borderRadius: radii.pill`, paddingHorizontal `lg`, paddingVertical `sm`, flexDirection row, alignItems center, gap `sm`. The icon is a `<Text>` rendering the emoji string from data — no third-party icon library. Used by every chip-bearing section. Do NOT extract to `src/components/` in this story (deferred to a later cleanup PR by user direction)."

      - "[dummy data] Create `src/data/sampleProfile.ts` exporting (a) a `Profile` TypeScript type and (b) a typed const `SAMPLE_PROFILE: Profile`. The Profile type defines: `name: string`, `age: number`, `verified: boolean`, `location: { flagEmoji: string; distanceKm: number; area: string }`, `photoUri: ImageSourcePropType` (use `require('@/assets/images/sample.jpg')`), `topChips: ChipDatum[]`, `similarities: { description: string; chips: ChipDatum[] }`, `aboutMe: ChipDatum[]`, `marriageIntentions: { matchLabel: string; intentionIcon: string; intentionLabel: string; timeframeLabel: string }`, `faith: ChipDatum[]`, `futurePlans: ChipDatum[]`, `interests: ChipDatum[]`, `personality: ChipDatum[]`, `education: ChipDatum[]`, `languages: ChipDatum[]`, `bio: string`, where `ChipDatum = { icon: string; label: string }`. Values must exactly match the screenshots: name `'Jasmin'`, age `28`, verified `true`, flagEmoji `'🇩🇪'`, distanceKm `276`, area `'Bogenhausen'`. Top chips: 🎓 Teacher, 🌙 Actively practising, 🌍 Arab, 🇯🇴 Jordanian. Similarities description: `'You and Jasmin already share similarities. Explore your connection more.'` with chip 🌙 Actively practising. About me: ↕ 173cm (5' 8\"), 💍 Never Married, 😊 Doesn't have children. Marriage intentions: Match! / 💍 / Marriage / 1-2 years. Faith: 🌙 Sunni, 🧕 Hijab, 🌙 Actively practising, 🍴 Only eats halal food, 🚭 Non-Smoker, 🍷 Doesn't drink, 🍴 Fasting. Future plans: ✈️ Won't move abroad. Interests: 🍫 Chocolate, ☕ Coffee, 👨‍🍳 Cooking, 🍽️ Eating out, 👪 Family time, 🏋️ Gym, 🥗 Healthy eating, 🌳 Nature walks, 🎨 Painting, 📷 Photography, 👫 Spending time with friends, ✈️ Travel, 🤝 Volunteering. Personality: 🌟 Active Listener, ❤️ Empathetic, 🙌 Respectful, 💡 Self-aware. Education: 🎓 Undergraduate degree, 💼 Teacher. Languages: 🌍 Arab, 🗣️ German, 🗣️ English, 🗣️ Arabic, 🌐 Grew up in Germany, 🇯🇴 Jordanian. Bio: '...' (intentional placeholder). Per-chip labels (Teacher, Sunni, etc.) live in this dataset, NOT in labels.json — they are data, not screen copy."

      - "[asset] Copy `C:/Users/syede/Claude-Master/.claude/screenshots/sample.jpg` into the project repo at `assets/images/sample.jpg` (create the `assets/images/` folder if it does not exist) and commit it. Reference it from `SAMPLE_PROFILE.photoUri` via `require('@/assets/images/sample.jpg')`. If `metro.config.js` requires asset-extension declarations, ensure `jpg` is recognized (default Metro config already includes jpg)."

      - "[labels.json policy] Add new label keys (each with at minimum the `en` field) for: `landing_header_verified_glyph` (the verified tick character, defaulted to `✓`), `landing_banner_overline` (`REVEAL JASMIN'S FILTERS`), `landing_banner_text` (`Change your mind and give Jasmin a second chance!`), `landing_banner_action_label` (`Like`), `landing_section_similarities_title` (`Your similarities`), `landing_section_about_title` (`About me`), `landing_section_marriage_intentions_title` (`Jasmin's marriage intentions`), `landing_section_faith_title` (`Faith`), `landing_section_future_plans_title` (`Future plans`), `landing_section_interests_title` (`Interests`), `landing_section_personality_title` (`Personality`), `landing_section_education_title` (`Education and career`), `landing_section_languages_title` (`Languages and ethnicity`), `landing_section_bio_title` (`Bio`), `landing_section_verified_profile_title` (`Verified profile`), `landing_section_verified_profile_body_template` (`{{name}}'s main photo has been verified.`), `landing_compliment_overline` (`COMPLIMENT`), `landing_compliment_title` (`Don't wait, chat with {{name}} now.`), `landing_compliment_placeholder` (`Let them know what you like about their profile`), `landing_compliment_submit_label` (`Submit`), `landing_action_share` (`Share profile`), `landing_action_favourite` (`Favourite`), `landing_action_block` (`Block`), `landing_action_report` (`Report`), `landing_match_label` (`Match!`). Templates ending in `_template` use `{{name}}` substitution at render time using `SAMPLE_PROFILE.name`. Per-chip labels are NOT added to labels.json (see [dummy data])."

      - "[theme + tokens] All colors come from `theme.colors.*` via `useTheme()`; all spacing from `theme.spacing.*`; all radii from `theme.radii.*` (pill for buttons + chips, lg for cards); typography from `textStyles.*`. The bottom banner gold band uses `theme.colors.text.premium` as backgroundColor; the inner banner card uses `theme.colors.bg.premium`. NO new theme tokens are introduced in this story unless the implementer can demonstrate an existing token cannot represent the design — if needed, add at most one new token and call it out in the PR description. The screen MUST work in both light and dark mode (no hardcoded hex literals in JSX or StyleSheets)."

      - "[stub-testID rename] The phase-3 stub testID `landing-screen-stub` is replaced with `landing-screen` on the LandingScreen root (the ScrollView). All other test files using the old testID — including but not limited to `__tests__/AppNavigator.test.tsx`, `__tests__/AuthGate.test.tsx`, `__tests__/AppRoutes.test.tsx`, `__tests__/AuthRoutes.test.tsx`, `__tests__/AuthRoutes.backbutton.test.tsx`, `__tests__/screens.stubs.test.tsx` — are updated in this same story. Implementer must run `grep -r 'landing-screen-stub' .` to confirm zero remaining references after the change. All previously-green tests remain green."

      - "[component tests — LandingScreen.test.tsx] In `__tests__/LandingScreen.test.tsx`, cover at minimum: (a) renders the testIDs `landing-screen`, `landing-header`, `landing-header-close`, `landing-header-title`, `landing-bottom-banner`, `landing-banner-like-button`, `landing-hero`; (b) the header title contains `SAMPLE_PROFILE.name`; (c) every section testID `landing-section-<slug>` listed above is in the tree (one assertion per section); (d) the chip count for each chip-bearing section equals `SAMPLE_PROFILE.<key>.length` (e.g., interests = 13, faith = 7, languages = 6); (e) tapping `landing-header-close` calls `useAuth().signOut` exactly once (use `jest.spyOn` on the AuthContext module — same pattern as LoginScreen.test.tsx story 5.2); (f) tapping `landing-banner-like-button`, `landing-compliment-submit`, and the three action-footer buttons does NOT throw (no-op coverage); (g) the rendered `<Image>` source equals `require('@/assets/images/sample.jpg')` (compare via Object.is on the require result); (h) `StyleSheet.flatten(landing-bottom-banner.props.style).backgroundColor` equals `lightColors.text.premium` when ThemeProvider is in light mode; (i) every chip in `SAMPLE_PROFILE.interests` is rendered with the exact `icon + label` shown in the dataset."

      - "[AppRoutes integration test] Update `__tests__/AppRoutes.test.tsx`: rendering `<AppRoutes />` with `isAuthenticated: true` mounts a tree containing testID `landing-screen` (not `landing-screen-stub`); the tab labelled `landing_tab_home_label` is still rendered; the bottom tab navigator from phase 3 story 3.4 remains visible (testID continuity)."

      - "[verification gates] Before opening the PR, all of the following pass: `npm run lint` (0/0), `npx prettier --check`, `npx tsc --noEmit`, `npx jest` (every suite green; total test count strictly greater than the count at branch-creation time). The Android emulator manual smoke check (story 6.3) runs after this story merges — story 6.1 itself does not block on emulator validation."

    notes: |
      Tensions and decisions to surface during the phase-6 brainstorm before dispatch:

      1. SCOPE DIVERGENCE FROM ARCHITECTURE.MD — architecture.md previously stated the v1 landing screen is empty (heading only). This story replaces that contract entirely with a styled profile-card layout sourced from the user's screenshots. Confirm this with the user before dispatching; if confirmed, the brainstorm should also propose a one-line update to architecture.md's testing strategy section so the manual E2E in story 6.3 references the new content.

      2. HEADER X TAP BEHAVIOR — currently spec'd to call signOut(). Alternative: X is a no-op (the user never leaves the post-auth state in v1). The signOut wiring is recommended because otherwise there is no path back to Login on a real device. If the user prefers the no-op behavior, drop the signOut AC and the corresponding test.

      3. COMPLIMENT TEXTINPUT IS BORDERED, NOT UNDERLINED — diverges from the underline-only input style enforced for Login/Register in PR #29. Acceptable: this is a different control class (multi-line composer textarea), not a form field.

      4. PREMIUM GOLD TOKENS — text.premium / bg.premium were introduced for premium-feature surfaces. We are reusing them for the bottom banner because they are the closest visual match. If the implementing subagent finds the rendered banner reads visibly different from the screenshot in light or dark mode, request a follow-up token addition rather than hand-editing the screen.

      5. STORY SIZE — this story is large enough to be a candidate for splitting. If the implementing subagent estimates more than ~6h of work, split into:
         6.1a layout root + header + hero + bottom banner + dummy data scaffold + asset
         6.1b scroll body sections (similarities → languages) + chip primitive
         6.1c bio + verified-profile card + compliment composer + actions footer + tests + AppRoutes update
         The split, if taken, is a brainstorm decision, not a blanket instruction.

      6. ASSET COPY — `sample.jpg` lives in `.claude/screenshots/` (workspace metadata). Copying it into `assets/images/` makes it part of the project repo. This is intentional — it is the v1 sample profile photo, not workspace metadata.

      7. TAB BAR vs. GOLD BANNER — phase 3 story 3.4 wired a bottom tab navigator that hosts LandingScreen as its only tab; that decision said "the tab bar remains visible." The user's screenshots do not show a tab bar — only the gold banner at the bottom. This is a real conflict. Three options: (a) hide the tab bar on the Landing route (via `screenOptions.tabBarStyle: { display: 'none' }` or similar), (b) keep the tab bar visible BELOW the gold banner (visually unusual but preserves phase-3 architecture), or (c) raise to the user for an explicit decision before implementing. Recommended: surface during the phase-6 brainstorm. The AppRoutes integration test AC in this story asserts the tab navigator is mounted; if option (a) is taken, the test should additionally assert the tab-bar UI is hidden — leave the navigator MOUNTED either way so phase-3 wiring is preserved.

  - id: 6.2
    title: Document the manual end-to-end test script
    agent: frontenddeveloper
    done: false
    depends_on: []
    acceptance_criteria:
      - "A new file `tests/manual-e2e.md` (or equivalent under the project root) is committed containing a step-by-step manual test script that a human runs on the Android emulator to validate the v1 happy path."

      - "The script covers, in order: (1) clear AsyncStorage / use a fresh AVD profile so the registered list starts empty; (2) launch `npx expo start` and open on the Android emulator; (3) on Login, tap the link to Register; (4) submit the Register form with a fresh valid record (e.g., `email: smoke@example.com, username: smoke, password: Smoke1234`); (5) verify the success modal renders with the expected title/body; (6) verify tap-outside does NOT dismiss; (7) tap OK and verify Login is now displayed; (8) log in with the same credentials; (9) verify the post-auth LandingScreen renders the Jasmin profile card and run every step-9 visual check (9a–9r) below; (10) seed-fallback path: relaunch with empty storage, log in directly with `testuser` / `Test@123`, then re-run step-9 checks (the LandingScreen renders the SAME static SAMPLE_PROFILE regardless of which user is signed in for v1, so the visual checks must reproduce identically)."

      - "Step-9 visual checks for LandingScreen (each produces one pass/fail entry in the results file): (9a) the hero photo from `assets/images/sample.jpg` renders edge-to-edge at the top of the screen; (9b) the transparent header overlays the top of the photo with the X close icon visible on the left and the title `Jasmin ✓` centered (white/inverse text legible against the photo); (9c) the gold bottom banner is fixed at the bottom of the screen and visible on first render with the crown overline `REVEAL JASMIN'S FILTERS` and the inner card containing `Change your mind and give Jasmin a second chance!` text plus the `Like` pill button; (9d) below the photo, name `Jasmin` + age `28` + verified-tick reads in light text over the darkened lower portion of the photo; (9e) the location line reads `🇩🇪 276 KM AWAY, BOGENHAUSEN`; (9f) the top chip row contains exactly four chips in order: 🎓 Teacher, 🌙 Actively practising, 🌍 Arab, 🇯🇴 Jordanian; (9g) scrolling reveals each section in this order with title and chip/card content matching the dataset: Your similarities → About me → Jasmin's marriage intentions → Faith → Future plans → Interests → Personality → Education and career → Languages and ethnicity → Bio → Verified profile → Compliment → Action footer; (9h) while scrolling, the header stays fixed at the top of the screen and remains transparent (scroll content visibly passes BEHIND it); (9i) while scrolling, the gold bottom banner stays fixed at the bottom and the bottom of the scroll content stops above the banner — the user can scroll to see the very last section without it being permanently hidden; (9j) the Marriage intentions card renders with `Match!` on the left in pink/red, a horizontal line, and the `Marriage` dark pill on the right with `1-2 years` caption beneath; (9k) the Verified profile card renders with the title + ⓘ info icon + body text `Jasmin's main photo has been verified.`; (9l) the Compliment section renders the `COMPLIMENT` overline, the heading `Don't wait, chat with Jasmin now.`, the multi-line text input with placeholder `Let them know what you like about their profile`, and the full-width pill `Submit` button below the input; (9m) the action footer renders the `Share profile` link with share icon, then a 3-column row of icon-over-label buttons `Favourite`, `Block`, `Report`; (9n) tapping the `Like` button on the gold banner does not crash (no-op confirmed); (9o) tapping `Submit` in the Compliment section with the input empty does not crash (no-op confirmed); (9p) tapping each of `Share profile`, `Favourite`, `Block`, `Report` does not crash (no-op confirmed); (9q) tapping the X close icon in the header signs the user out — the app navigates back to LoginScreen; (9r) signing in again (`testuser` / `Test@123`) returns to LandingScreen and reproduces every check above."

      - "Each step states explicitly what the user should see (the expected observation) so the test produces a pass/fail result, not a free-form impression."

      - "The script also lists the negative checks the architecture mandates: (a) on Register, submit with mismatched confirm password — expect inline mismatch error and NO modal; (b) on Login, submit wrong credentials — expect inline `login_invalid_credentials` text below the button; (c) start typing in either field on Login — expect the inline error to clear; (d) on Login with one or both fields empty, the `Sign In` button is visibly disabled (faded background using the disabled accent token from PR #29) and tapping it does nothing; (e) on Register with any of the four fields empty, the `Create Account` button is visibly disabled and tapping it does nothing."

      - "The script lists the back-button checks: hardware back from RegisterScreen returns to Login; hardware back while the success modal is open dismisses the modal AND navigates to Login (same as OK); hardware back from LandingScreen exits the app (the OS-level back behavior — no parent screen in the post-auth tab navigator); tapping the X close icon on LandingScreen calls `signOut()` and returns to Login (covered by step 9q above; included here for symmetry with the hardware-back contract)."
    notes: |
      The tab-bar vs. gold-banner conflict (story 6.1 note 7) determines what step-9 should observe at the very bottom of the screen. If option (a) is taken (tab bar hidden on Landing), step 9c is correct as written. If option (b) is taken (tab bar visible BELOW the banner), step 9c must be amended to also observe the tab bar below the gold banner. Reconcile after the phase-6 brainstorm before this script is finalized.

  - id: 6.3
    title: Execute the manual E2E test on the Android emulator and record the result
    agent: frontenddeveloper
    done: false
    depends_on:
      - 6.1
      - 6.2
    acceptance_criteria:
      - "The user (or the agent under user observation) runs the script from story 6.2 against the Android emulator, top to bottom."

      - "Each numbered step in the script — including every step-9 visual check (9a through 9r), every step-10 seed-fallback re-check, every negative check (a–e), and every back-button check — produces one pass/fail line in a results record committed to `tests/manual-e2e-results-<ISO-date>.md`. Each line names the check id (e.g., `9j`, `negative-d`, `back-2`) so the results file is greppable."

      - "Every happy-path step passes, every step-9 visual check matches the SAMPLE_PROFILE dataset and the screenshots in `.claude/screenshots/`, every negative check produces the expected error UX (including the disabled-button states from PR #29), and every back-button check matches the architecture contract (including the X close → signOut path on LandingScreen)."

      - "If any step fails, the story is NOT closed. The failure is diagnosed and either (a) fixed in the same phase via a follow-up story whose scope is bounded to the failure, or (b) converted to a phase-6 blocker entry in `context.md` Active blockers, with the user deciding next action."

      - "A short note is added to `context.md` Recent changes summarizing the result (counts of pass/fail lines per category — happy-path, step-9 visual, negative, back-button) and linking the results file."
    notes: "This story is the only place in the project where a human-in-the-loop assertion gates phase completion. The story-6.1 implementation should land first (the LandingScreen content this script validates does not exist until 6.1 is merged), and 6.2 must be merged before 6.3 dispatches (the script being executed is the artifact 6.2 produces)."
